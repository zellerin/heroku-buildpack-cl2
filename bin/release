#!/bin/sh



cat << EOF
---
addons:
  - shared-database:5mb
config_vars:
  PATH: bin:/usr/bin:/bin
  CCL_DEFAULT_DIRECTORY: ccl  
default_process_types:
  web: ccl/scripts/ccl64 -l setup/run.lisp
EOF
